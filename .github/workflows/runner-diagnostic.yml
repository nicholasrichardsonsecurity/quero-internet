name: Runner Diagnostic

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ping:
    runs-on: self-hosted
    steps:
      - name: Runner started
        run: echo "runner-ok"
